name: Generate Tempo Traveller Jaipur JPG
on:
  push:
    paths:
      - 'assets/images/gmb/source/tempo-traveller-jaipur.svg'
      - '.github/workflows/generate-tempo-traveller-jaipur-jpg.yml'
  workflow_dispatch:
permissions:
  contents: write
jobs:
  convert:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Convert SVG to 16:9 HD JPG
        run: |
          sudo apt-get update
          sudo apt-get install -y imagemagick librsvg2-bin
          mkdir -p assets/images/gmb assets/images/blog
          rsvg-convert -w 1920 -h 1080 -o /tmp/tempo-traveller-jaipur.png assets/images/gmb/source/tempo-traveller-jaipur.svg
          convert /tmp/tempo-traveller-jaipur.png -background white -alpha remove -alpha off -sampling-factor 4:4:4 -quality 95 assets/images/gmb/tempo-traveller-jaipur.jpg
          cp assets/images/gmb/tempo-traveller-jaipur.jpg assets/images/blog/tempo-traveller-jaipur.jpg
      - name: Commit JPG
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add assets/images/gmb/tempo-traveller-jaipur.jpg assets/images/blog/tempo-traveller-jaipur.jpg
          git diff --cached --quiet || git commit -m "Generate Tempo Traveller Jaipur HD image"
          git pull --rebase origin main
          git push origin HEAD:main
